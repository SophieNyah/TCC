
%term ADD
%term SUB
%term MEM
%nonterm mais
%nonterm armoss
%nonterm mem

%%

$! regra $% [ADD $[regA, regB, regC$];SUB $[reg1, reg2$]] 3 <- mais({ int main(){int a,b=3; int c=b+2;} } armoss(s1, s2), moi) $% { return 3; } $!
$! load $% [ADD $[ s1, s2 $]; MEM $[ s3, s2 $]] <- mem( mais( A, B ), C ) $% { return $cost[1] + 2; } $!

%%

{

int main(int argc, char** argv){
    return 0;
}

}
