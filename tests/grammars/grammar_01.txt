
%term ADD
%term SUB
%term MEM
%nonterm mais
%nonterm armoss
%nonterm mem

%%

$! regra $% [ADD $[regA, regB, regC$]] 3 <- mais({ int main(){int a,b=3; int c=b+2;} } r1, c1) $% { $cost[0] + 3 } $!

$! load $% [ADD $[ s1, s2 $]; MEM $[ s3, s2 $]] <- mem( mais( r1, r2 ), r3 ) $% { $cost[1] + 2 } $!

%%

{

int main(int argc, char** argv){
    return 0;
}

}
